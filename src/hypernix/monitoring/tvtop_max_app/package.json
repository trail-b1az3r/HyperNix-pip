{
  "name": "tvtop-max",
  "version": "0.72.6",
  "description": "tvtop-max: tvtop-pro on OpenTUI, with the run's script, model, optimizer, logs and warnings",
  "private": true,
  "type": "module",
  "bin": {
    "tvtop-max": "src/index.ts"
  },
  "scripts": {
    "start": "bun run src/index.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "bun": ">=1.3.0"
  },
  "dependencies": {
    "@opentui/core": "0.5.12"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
